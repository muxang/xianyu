# ============= Database =============
POSTGRES_PASSWORD=dev_password_change_me
# Must use +asyncpg driver (async engine requirement)
DATABASE_URL=postgresql+asyncpg://xianyu:CHANGE_ME@localhost:5432/xianyu
LANGFUSE_DB_PASSWORD=langfuse_dev_password
LANGFUSE_NEXTAUTH_SECRET=change-me-in-prod-at-least-32-chars-long
LANGFUSE_SALT=change-me-too-at-least-32-characters

# ============= Redis =============
# DB 0 = production data (queues, cache, session state)
# DB 15 = reserved for integration tests (smoke scripts use this to avoid polluting dev data)
REDIS_URL=redis://localhost:6379/0

# ============= LLM APIs =============
# 阿里云百炼
DASHSCOPE_API_KEY=sk-your-dashscope-key

# DeepSeek (备用)
DEEPSEEK_API_KEY=sk-your-deepseek-key

# ============= 飞书 =============
FEISHU_APP_ID=cli_xxxxxxx
FEISHU_APP_SECRET=xxxxxxx
FEISHU_VERIFICATION_TOKEN=xxxxxx
FEISHU_ENCRYPT_KEY=
FEISHU_ADMIN_OPEN_ID=ou_xxxxxx
FEISHU_ADMIN_CHAT_ID=oc_xxxxxx

# ============= Langfuse =============
LANGFUSE_PUBLIC_KEY=pk-lf-xxxxx
LANGFUSE_SECRET_KEY=sk-lf-xxxxx
LANGFUSE_HOST=http://localhost:3100

# ============= Application =============
APP_ENV=development
APP_LOG_LEVEL=INFO
APP_SECRET_KEY=change-me-for-jwt-at-least-32-characters-long
APP_WORKSPACE_USERNAME=admin
APP_WORKSPACE_PASSWORD=admin_change_me

# ============= 加密 =============
# 用以下 Python 代码生成：
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
COOKIE_ENCRYPTION_KEY=generate-a-fernet-key-using-python
